export enum ApplicationSide {
  frontend = 'frontend',
  backend = 'backend',
}

export enum Stage {
  develop = 'develop',
  production = 'production',
}

export enum ComparisonResult {
  Equal,
  Greater,
  Less,
}
